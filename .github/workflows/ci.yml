name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  BUN_VERSION: "1.3.14"

jobs:
  static:
    name: Lint, comments, types
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - run: bun install --frozen-lockfile
      - name: Biome lint and format check
        run: bun run lint
      - name: No-comment policy
        run: bun run check-comments
      - name: Source byte policy
        run: bun run check-bytes
      - name: No Bun built-ins in shipped server code
        run: bun run check-bun-imports
      - name: One copy of every overridden dependency
        run: bun run check-deps
      - name: Typecheck
        run: bun run typecheck

  test:
    name: Unit and integration tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: tack
          POSTGRES_PASSWORD: tack
          POSTGRES_DB: tack
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U tack"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:8-alpine
        ports:
          - 6380:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      DATABASE_URL: postgres://tack:tack@localhost:5433/tack
      REDIS_URL: redis://localhost:6380
      BETTER_AUTH_SECRET: ci-secret-0123456789abcdef0123456789abcdef
      BETTER_AUTH_URL: http://localhost:3000
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NEXT_PUBLIC_REALTIME_URL: ws://localhost:3100
      GITHUB_APP_SLUG: tack-ci
      GITHUB_APP_ID: "1"
      GITHUB_APP_CLIENT_ID: Iv1.ci
      GITHUB_APP_CLIENT_SECRET: ci-github-app-client-secret
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - run: bun install --frozen-lockfile
      - name: Mint a throwaway signing key for the GitHub connect tests
        run: |
          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out /tmp/tack-ci-app-key.pem
          {
            echo "GITHUB_APP_PRIVATE_KEY<<TACK_CI_KEY_EOF"
            cat /tmp/tack-ci-app-key.pem
            echo "TACK_CI_KEY_EOF"
          } >> "$GITHUB_ENV"
      - name: Create an isolated database per package
        run: |
          for db in tack_test_core tack_test_svc tack_test_rt tack_test_rts tack_test_mcp tack_test_web; do
            PGPASSWORD=tack createdb -h localhost -p 5433 -U tack "$db"
          done
      - name: Push the schema to every database
        run: |
          for db in tack tack_test_core tack_test_svc tack_test_rt tack_test_rts tack_test_mcp tack_test_web; do
            DATABASE_URL="postgres://tack:tack@localhost:5433/$db" bun run --filter '@tack/db' push
          done
      - name: Run tests
        run: bun run test

  schema:
    name: Migrations match the schema
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: tack
          POSTGRES_PASSWORD: tack
          POSTGRES_DB: tack
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U tack"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      DATABASE_URL: postgres://tack:tack@localhost:5433/tack_from_migrations
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - run: bun install --frozen-lockfile
      - name: A database built the way someone installing this would build one
        run: |
          PGPASSWORD=tack createdb -h localhost -p 5433 -U tack tack_from_migrations
          bun run --filter '@tack/db' migrate
      - name: The committed migrations carry everything the schema declares
        run: bun run db:check-drift

  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgres://tack:tack@localhost:5433/tack
      REDIS_URL: redis://localhost:6380
      BETTER_AUTH_SECRET: ci-secret-0123456789abcdef0123456789abcdef
      BETTER_AUTH_URL: http://localhost:3000
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NEXT_PUBLIC_REALTIME_URL: ws://localhost:3100
      TACK_PASSWORD_AUTH: "true"
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - run: bun install --frozen-lockfile
      - run: bun run build
      - name: Smoke test the copied standalone artifact
        run: |
          artifact_root="$(mktemp -d)"
          log_path="$RUNNER_TEMP/standalone.log"
          server_pid=""
          cleanup() {
            exit_code=$?
            if [ -n "$server_pid" ]; then
              kill "$server_pid" 2>/dev/null || true
              wait "$server_pid" 2>/dev/null || true
            fi
            if [ "$exit_code" -ne 0 ]; then cat "$log_path" || true; fi
            exit "$exit_code"
          }
          trap cleanup EXIT
          mkdir -p "$artifact_root/tack/apps/web/.next"
          cp -R apps/web/.next/standalone "$artifact_root/tack/apps/web/.next/standalone"
          cp apps/web/package.json "$artifact_root/tack/apps/web/package.json"
          {
            echo "BETTER_AUTH_SECRET=standalone-smoke-0123456789abcdef0123456789abcdef"
            echo "BETTER_AUTH_URL=http://127.0.0.1:3001"
            echo "NEXT_PUBLIC_APP_URL=http://127.0.0.1:3001"
          } > "$artifact_root/tack/.env"
          (
            cd "$artifact_root/tack/apps/web"
            unset BETTER_AUTH_SECRET BETTER_AUTH_URL NEXT_PUBLIC_APP_URL TACK_PASSWORD_AUTH
            unset GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET GITHUB_CLIENT_ID GITHUB_CLIENT_SECRET
            unset RESEND_API_KEY EMAIL_FROM TACK_DEV_LOGIN
            PORT=3001 HOSTNAME=127.0.0.1 exec bun run start
          ) > "$log_path" 2>&1 &
          server_pid=$!
          for attempt in $(seq 1 200); do
            if ! kill -0 "$server_pid" 2>/dev/null; then break; fi
            if curl -fsS --connect-timeout 0.2 --max-time 0.2 http://127.0.0.1:3001/api/health 2>/dev/null | grep -q '"status":"ok"'; then
              echo "Standalone server became healthy with an unusable authentication configuration" >&2
              exit 1
            fi
            sleep 0.1
          done
          if kill -0 "$server_pid" 2>/dev/null; then
            echo "Standalone server accepted an unusable authentication configuration" >&2
            exit 1
          fi
          if wait "$server_pid"; then
            echo "Standalone server accepted an unusable authentication configuration" >&2
            exit 1
          fi
          server_pid=""
          grep -Fq 'Production requires a usable first-login method.' "$log_path"
          echo "TACK_PASSWORD_AUTH=true" >> "$artifact_root/tack/.env"
          : > "$log_path"
          (
            cd "$artifact_root/tack/apps/web"
            unset BETTER_AUTH_SECRET BETTER_AUTH_URL NEXT_PUBLIC_APP_URL TACK_PASSWORD_AUTH
            unset GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET GITHUB_CLIENT_ID GITHUB_CLIENT_SECRET
            unset RESEND_API_KEY EMAIL_FROM TACK_DEV_LOGIN
            PORT=3001 HOSTNAME=127.0.0.1 exec bun run start
          ) > "$log_path" 2>&1 &
          server_pid=$!
          for attempt in $(seq 1 60); do
            if curl -fsS --connect-timeout 1 --max-time 2 http://127.0.0.1:3001/api/health 2>/dev/null | grep -q '"status":"ok"'; then
              break
            fi
            sleep 1
          done
          curl -fsS --connect-timeout 2 --max-time 10 http://127.0.0.1:3001/api/health | grep -q '"status":"ok"'
          curl -fsS --connect-timeout 2 --max-time 10 --output /dev/null http://127.0.0.1:3001/logo.png
          static_file="$(find "$artifact_root/tack/apps/web/.next/standalone/apps/web/.next/static/chunks" -type f -name '*.js' -print -quit)"
          test -n "$static_file"
          static_path="${static_file#"$artifact_root/tack/apps/web/.next/standalone/apps/web/.next"}"
          curl -fsS --connect-timeout 2 --max-time 10 --output /dev/null "http://127.0.0.1:3001/_next$static_path"

  e2e:
    name: ${{ matrix.inbox == 'legacy' && 'End-to-end (Playwright)' || 'End-to-end (Inbox conversations)' }}
    strategy:
      fail-fast: false
      matrix:
        inbox: [legacy, conversations]
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: tack
          POSTGRES_PASSWORD: tack
          POSTGRES_DB: tack
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U tack"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:8-alpine
        ports:
          - 6380:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      DATABASE_URL: postgres://tack:tack@localhost:5433/tack
      TACK_SEED_CONFIRM_TARGET: localhost:5433/tack:user-sha256:4fa1a13ac468ac495f3390e859d76d5e8ef49806815b45a21de7711bcc624194
      REDIS_URL: redis://localhost:6380
      BETTER_AUTH_SECRET: ci-secret-0123456789abcdef0123456789abcdef
      BETTER_AUTH_URL: http://localhost:3000
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      NEXT_PUBLIC_REALTIME_URL: ws://localhost:3100
      REALTIME_PORT: "3100"
      TACK_DEV_LOGIN: "1"
      TACK_E2E_BASE_URL: http://localhost:3000
      NOTIFICATION_CONVERSATIONS_ENABLED: ${{ matrix.inbox == 'conversations' && 'true' || 'false' }}
      S3_ENDPOINT: http://localhost:9010
      S3_REGION: us-east-1
      S3_BUCKET: tack-uploads
      S3_ACCESS_KEY_ID: tackminio
      S3_SECRET_ACCESS_KEY: tackminio
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - run: bun install --frozen-lockfile
      - name: The dev scripts read the repository env file
        run: |
          {
            echo "DATABASE_URL=$DATABASE_URL"
            echo "TACK_SEED_CONFIRM_TARGET=$TACK_SEED_CONFIRM_TARGET"
            echo "REDIS_URL=$REDIS_URL"
            echo "BETTER_AUTH_SECRET=$BETTER_AUTH_SECRET"
            echo "BETTER_AUTH_URL=$BETTER_AUTH_URL"
            echo "NEXT_PUBLIC_APP_URL=$NEXT_PUBLIC_APP_URL"
            echo "NEXT_PUBLIC_REALTIME_URL=$NEXT_PUBLIC_REALTIME_URL"
            echo "REALTIME_PORT=$REALTIME_PORT"
            echo "TACK_DEV_LOGIN=$TACK_DEV_LOGIN"
            echo "TACK_E2E_BASE_URL=$TACK_E2E_BASE_URL"
            echo "NOTIFICATION_CONVERSATIONS_ENABLED=$NOTIFICATION_CONVERSATIONS_ENABLED"
            echo "S3_ENDPOINT=$S3_ENDPOINT"
            echo "S3_REGION=$S3_REGION"
            echo "S3_BUCKET=$S3_BUCKET"
            echo "S3_ACCESS_KEY_ID=$S3_ACCESS_KEY_ID"
            echo "S3_SECRET_ACCESS_KEY=$S3_SECRET_ACCESS_KEY"
          } > .env
      - name: Start object storage the same way the compose file does
        run: |
          docker run -d --name tack-minio -p 9010:9000 \
            -e MINIO_ROOT_USER=tackminio \
            -e MINIO_ROOT_PASSWORD=tackminio \
            quay.io/minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e server /data
          for attempt in $(seq 1 60); do
            if curl -fsS http://localhost:9010/minio/health/live >/dev/null; then
              echo "minio is up after ${attempt} attempts"
              docker run --rm --network host --entrypoint sh quay.io/minio/mc@sha256:a7fe349ef4bd8521fb8497f55c6042871b2ae640607cf99d9bede5e9bdf11727 -c \
                "mc alias set tack http://localhost:9010 tackminio tackminio && mc mb --ignore-existing tack/tack-uploads"
              exit 0
            fi
            sleep 2
          done
          echo "minio never became healthy" >&2
          docker logs tack-minio >&2 || true
          exit 1
      - name: Apply the schema to the e2e database
        run: bun run --filter '@tack/db' push
      - name: Install the Playwright browser
        working-directory: apps/web
        run: bunx playwright install --with-deps chromium
      - name: Boot the realtime and web servers
        run: |
          nohup bun run --filter '@tack/realtime' dev > "$RUNNER_TEMP/realtime.log" 2>&1 &
          disown
          nohup bun run --filter '@tack/web' dev > "$RUNNER_TEMP/web.log" 2>&1 &
          disown
      - name: Wait for the web server to answer its health check
        run: |
          for attempt in $(seq 1 90); do
            if curl -fsS http://localhost:3000/api/health | grep -q '"status":"ok"'; then
              echo "web is up after ${attempt} attempts"
              exit 0
            fi
            sleep 2
          done
          echo "web never became healthy" >&2
          cat "$RUNNER_TEMP/web.log" >&2 || true
          exit 1
      - name: Wait for the realtime server to answer its health check
        run: |
          for attempt in $(seq 1 60); do
            if curl -fsS "http://localhost:${REALTIME_PORT}/health" >/dev/null 2>&1; then
              echo "realtime is up after ${attempt} attempts"
              exit 0
            fi
            sleep 2
          done
          echo "realtime never became healthy" >&2
          cat "$RUNNER_TEMP/realtime.log" >&2 || true
          exit 1
      - name: Run the end-to-end suite
        if: matrix.inbox == 'legacy'
        run: bun run test:e2e
      - name: Run grouped inbox end-to-end tests
        if: matrix.inbox == 'conversations'
        working-directory: apps/web
        run: bunx playwright test e2e/notification-conversations.spec.ts
      - name: Show what the servers logged
        if: failure()
        run: |
          echo "::group::web"
          cat "$RUNNER_TEMP/web.log" || true
          echo "::endgroup::"
          echo "::group::realtime"
          cat "$RUNNER_TEMP/realtime.log" || true
          echo "::endgroup::"
      - name: Upload Playwright traces
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-traces-${{ matrix.inbox }}
          path: apps/web/test-results/
          if-no-files-found: ignore
      - name: Dump server logs on failure
        if: failure()
        run: |
          echo "=== web ==="; cat "$RUNNER_TEMP/web.log" || true
          echo "=== realtime ==="; cat "$RUNNER_TEMP/realtime.log" || true
