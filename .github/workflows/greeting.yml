name: Greet first time contributors

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:
  greet:
    name: Say hello
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/first-interaction@1c4688942c71f71d4f5502a26ea67c331730fa4d
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          issue_message: |
            Thanks for opening your first issue on Tack.

            A maintainer will triage this. If it is a bug, the fastest way to move it
            along is exact steps to reproduce starting from the seeded demo workspace,
            since that is what we run locally.

            If you fancy fixing it yourself, [CONTRIBUTING.md](https://github.com/Thefirstmannnn/tack/blob/main/CONTRIBUTING.md)
            walks through the setup. You need Bun and Docker, and nothing else.
          pr_message: |
            Thanks for your first pull request to Tack.

            Two things that will save you a review round: `bun run verify` runs the
            same four checks CI does, and the repo has no comments in code by policy,
            so `bun run check-comments` will flag any you added out of habit.

            A maintainer will review this shortly. Ask anything on the thread.
